/*
 * Lesson 05: Classes and Objects
 * Lección 05: Clases y Objetos
 *
 * Goal:
 * Create an object from the Player class and call its methods from Main.
 *
 * Meta:
 * Crear un objeto de la clase Player y llamar a sus métodos desde Main.
 *
 * Context:
 * Main is the place where the program starts. Player is the class that models
 * one participant. Main should use Player; it should not store all player data
 * by itself.
 *
 * Contexto:
 * Main es el lugar donde comienza el programa. Player es la clase que modela
 * a un participante. Main debería usar Player; no debería almacenar todos los
 * datos del jugador por sí mismo.
 *
 * Student work / Trabajo del estudiante:
 * 1. Complete Player.java first.
 *    Completa Player.java primero.
 * 2. Create one Player object in main.
 *    Crea un objeto Player en main.
 * 3. Print the player's name and score using methods.
 *    Imprime el nombre y el puntaje del jugador usando métodos.
 * 4. Add score once and print the new value.
 *    Agrega puntaje una vez e imprime el nuevo valor.
 */

public class Main {
    public static void main(String[] args) {
        // TODO: Create your Player object here.
        // Example structure: Player player = new Player("Ana");
        //
        // Crea tu objeto Player aquí.
        // Estructura de ejemplo: Player player = new Player("Ana");
        
    }
}
